<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.be2.monapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".infovisiteur"></activity>
        <activity android:name=".parametre" />
        <activity android:name=".envoidesdonneesvers" />
        <activity android:name=".menuprincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".synthesedumois" />
        <activity android:name=".fraishorsforfait" />
        <activity android:name=".fraisauforfait" />
    </application>

</manifest>